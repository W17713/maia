/*const aggregate = require('../aggregator');
const agg = new aggregate;

test('test return array to contain userid', ()=>{
    
    return agg.get('Highlights').then(data => {
        expect(data).toContain('userid');
    });
});*/